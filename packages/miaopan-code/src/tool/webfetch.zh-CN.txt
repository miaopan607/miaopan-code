- 获取指定 URL 的内容
- 接受 URL 和可选格式作为输入
- 获取 URL 内容并转换为所请求的格式（默认为 markdown）
- 以指定格式返回内容
- 需要检索并分析网页内容时使用此工具

使用说明：
  - 重要：如果有其他工具能提供更好的网页抓取能力、更适合该任务或限制更少，请优先使用该工具而不是此工具。
  - URL 必须是格式完整的有效 URL
  - HTTP URL 会自动升级为 HTTPS
  - 格式选项："markdown"（默认）、"text" 或 "html"
  - 此工具是只读的，不会修改任何文件
  - 如果内容很大，结果可能会经过总结
