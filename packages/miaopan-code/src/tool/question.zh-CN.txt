当你需要在执行过程中向用户提问时，请使用此工具。你可以借此：
1. 收集用户偏好或要求
2. 澄清含糊不清的指示
3. 在工作过程中获取有关实现选择的决定
4. 向用户提供选项，让其决定要采取的方向。

使用说明：
- 启用 `custom` 时（默认启用），系统会自动添加“输入自定义答案”选项；不要包含“其他”或兜底选项
- 答案以标签数组的形式返回；设置 `multiple: true` 可允许选择多个答案
- 如果你推荐某个特定选项，请将其放在列表第一项，并在标签末尾添加“（推荐）”
