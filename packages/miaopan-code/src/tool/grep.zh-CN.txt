- 适用于任意规模代码库的快速内容搜索工具
- 使用正则表达式搜索文件内容
- 支持完整的正则表达式语法（例如 "log.*Error"、"function\s+\w+" 等）
- 使用 include 参数按模式筛选文件（例如 "*.js"、"*.{ts,tsx}"）
- 返回包含匹配行的文件路径和行号
- 需要查找包含特定模式的文件时使用此工具
- 如果需要识别或统计文件中的匹配数量，请使用 Bash 工具直接运行 `rg`（ripgrep）。不要使用 `grep`。
- 执行可能需要多轮 glob 和 grep 的开放式搜索时，请改用 Task 工具
