将内容写入本地文件。

使用说明：
- 如果目标路径已有文件，此工具会覆盖该文件。
- 对已有文件，必须先使用 Read 工具读取内容；未先读取时工具会失败。
- 始终优先编辑代码库中的现有文件。除非用户明确要求，否则不要创建新文件。
- 除非用户明确要求，否则不要主动创建文档文件（*.md）或 README 文件。
- 只有用户明确要求时才使用表情符号；除非用户要求，否则不要将表情符号写入文件。
