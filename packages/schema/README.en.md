# @miaopan-code/schema

Languages: [简体中文](README.md) · English

`@miaopan-code/schema` defines the shared Effect/JSON schemas consumed by Protocol, Core, Server, and the generated SDK. Keep schema fields and encoded values compatible with the public API. This package is private to the workspace.
