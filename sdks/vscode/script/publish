#!/usr/bin/env bash
set -euo pipefail

version=$(node -p "require('./package.json').version")
mkdir -p dist
vsce package --no-git-tag-version --no-update-package-json --no-dependencies -o dist/miaopan-code.vsix "$version"
